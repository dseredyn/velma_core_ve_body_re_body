# subsystem_buffer_parent_container{Command}

float64 i       # subsystem_buffer{type: port; validity: i_valid}
bool i_valid

float64 q       # subsystem_buffer{type: port; validity: q_valid}
bool q_valid

float64 dq      # subsystem_buffer{type: port; validity: dq_valid}
bool dq_valid

